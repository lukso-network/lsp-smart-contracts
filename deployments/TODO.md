# Deployments TODO

- [] The `FOUNDRY_PROFILE=deployments` use solc version 0.8.17, but the latest versions of LSP7/8 Mintable Init and LSP7/8 Customizable Token Init compile the contracts with 0.8.28 and different optimizer settings.

- [] Change the file name + logic of `generate-dummy-artifact.sh` to make it general purpose

- [x] Make the contract verification 3 x bash scripts with specific names to simplify the README instructions.
